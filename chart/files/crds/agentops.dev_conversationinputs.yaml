---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: conversationinputs.agentops.dev
spec:
  group: agentops.dev
  names:
    kind: ConversationInput
    listKind: ConversationInputList
    plural: conversationinputs
    shortNames:
    - convin
    singular: conversationinput
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.conversationRef.name
      name: Conversation
      type: string
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .status.consumed
      name: Consumed
      type: boolean
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ConversationInput is an out-of-line payload for a Conversation
          input item.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ConversationInputSpec carries a large out-of-line work-unit
              payload.
            properties:
              conversationRef:
                description: ObjectRef references another object by name (same namespace).
                properties:
                  name:
                    description: Name of the referenced object.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              payload:
                type: string
              type:
                description: InputType classifies a work unit.
                enum:
                - task
                - alert
                - reply
                - recurrence
                - job
                type: string
            required:
            - conversationRef
            - payload
            - type
            type: object
          status:
            description: ConversationInputStatus tracks consumption.
            properties:
              consumed:
                type: boolean
              consumedAt:
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
