## 1. The environment, defined by the tree (design D3, D6, D7, D9)

- [ ] 1.1 Write `.github/scripts/cloud-bootstrap.sh`: exits 0 at once unless `CLAUDE_CODE_REMOTE` is `1` or `true`; one `## <tool>` section each for helm, `@fission-ai/openspec@1.10.0`, pyyaml, the envtest assets (into `~/.envtest`, printing the `KUBEBUILDER_ASSETS` path), serena, and a Go ≥ 1.25 check that installs the official tarball only when the image's is older — each section check → install → verify, none fatal to the others, every missing tool named on stderr at the end, exit 0 always. Verify: `bash -n` passes; with the marker unset the script installs nothing and exits 0; with the marker set and every binary stubbed present it runs no installer.
- [ ] 1.2 Add `--verify` to the same script: installs nothing, prints `present`/`missing` per tool to stdout, exit 0; and add it to `.claude/settings.json` as a `SessionStart` hook (matcher `""`, timeout 10). Verify: `cloud-bootstrap.sh --verify` with the marker unset prints nothing; with it set and helm stubbed absent prints `helm missing`; `python3 -c 'import json;json.load(open(".claude/settings.json"))'` passes.
- [ ] 1.3 Rewrite the `serena` and `docker` entries of `.mcp.json` as `bash -c` wrappers per D7 — exec directly when the binary is on the path, otherwise remote-only wait up to 180 s then exec, never `exit 0` locally; leave `sonarqube` as is. Verify: `python3 -c 'import json;json.load(open(".mcp.json"))'` passes; on this workstation both servers connect as before (`claude mcp list`, or a session start showing them connected).
- [ ] 1.4 Write `.claude/rules/remote-session.md` (unscoped, own `## ` heading): the environment's name and the setup field's two lines verbatim; what the bootstrap installs and what the verify hook prints; the clone is the working copy — `git checkout -b change/<name> origin/master` in place, never `git worktree add`; the fire flow and the `autoimplement` label in one table; what is workstation-only (deploy, `helmfile`, the visual check, the local cluster) and the sentence a pull request must carry about it; e2e is `gh workflow run e2e-smoke.yml --ref change/<name>`; variables are public and a secret is an API credential; the routine push rules as measured. Verify: the file names every item above and `.claude/rules/authoring.md`'s shape rules hold (tables and bold-lead bullets, no prose paragraph of three rules).
- [ ] 1.5 Correct `.claude/rules/build-test.md`: "this workstation has no Go toolchain" becomes "run the toolchain on your path; the container below is for a workstation that has none, and is the ONLY container" — keeping the container recipe and its four paid-for details intact; and `.claude/rules/gotchas.md` gains one bullet with the measured routine push rules and the public-variables fact. Verify: `grep -c 'no Go toolchain' .claude/rules/build-test.md` is 0 and the container section is unchanged in length ±2 lines.
- [ ] 1.6 Reword the delivery sentence in `openspec/config.yaml` (`rules.tasks`, "from its own git worktree") and in `.claude/commands/opsx/apply.md` step 1.5 and `.claude/skills/openspec-apply-change`'s matching step to "from its own git worktree — or, in a remote session, the session's clone with `change/<name>` checked out in place". Verify: `grep -n 'remote session' openspec/config.yaml .claude/commands/opsx/apply.md .claude/skills/openspec-apply-change/SKILL.md` finds each.

## 2. The label and the fire (design D1, D2, D5, D8)

- [ ] 2.1 `.github/review-triage.json`: add `"implement_label": "autoimplement"` and a `_comment` paragraph stating it is issue-level consent to implement AND the owner's word for `autofix` on the resulting pull request, gated on write access like the rest. Verify: `python3 -c 'import json;print(json.load(open(".github/review-triage.json"))["implement_label"])'` prints `autoimplement`.
- [ ] 2.2 Write `.github/scripts/remote-implement.py`: reads the `issues.labeled` event payload from `GITHUB_EVENT_PATH`; exits 0 doing nothing unless the label is the implement label from `review-triage.json`; asks `gh api repos/$R/collaborators/<sender>/permission` and on anything but `admin|maintain|write` removes the label and comments who may place it (one fixed sentence); otherwise POSTs `{"text": "<number>"}` to `$ROUTINE_FIRE_URL` with `Authorization: Bearer $ROUTINE_FIRE_TOKEN`, the `anthropic-beta: experimental-cc-routine-2026-04-01` and `anthropic-version` headers, and comments ONCE with the returned session URL under a `<!-- remote-implement:fired -->` marker; a non-2xx fire comments the status and fails the job. The fire is via `urllib` with the endpoint overridable by `--fire-url` for the test. Verify: the suite in 5.2.
- [ ] 2.3 Write `.github/workflows/remote-implement.yml`: `on: issues: types: [labeled]`, top-level `permissions: {}`, one job with `issues: write` only, `if: github.event.label.name == 'autoimplement'`, checkout, setup-python, `pip install pyyaml` not needed (stdlib only), run the script with `ROUTINE_FIRE_URL: ${{ vars.ROUTINE_FIRE_URL }}` and `ROUTINE_FIRE_TOKEN: ${{ secrets.ROUTINE_FIRE_TOKEN }}` and `GH_TOKEN: ${{ github.token }}`; a header comment stating why the hop exists (D1). Verify: `python3 -c 'import yaml;yaml.safe_load(open(".github/workflows/remote-implement.yml"))'` passes; the existing `review-dispatch.test.sh` trigger-set assertion still passes (no `pull_request_target` anywhere).
- [ ] 2.4 Write `.github/routines/implement-issue.md`: the process the routine follows — validate the payload is a number; `gh issue view` it; derive `<n>-<kebab-title>` as the change name; if `openspec/changes/*/.github-issue` already holds the number continue that change, and if a pull request from its branch is open stop with one comment; else `/opsx:propose` seeded from the issue, `opsx-issue.sh open <name> --promote <n>`, `git checkout -b change/<name> origin/master` in place, `/opsx:apply` through every task including the three trailing sections, `gh workflow run e2e-smoke.yml --ref change/<name>` and wait on its conclusion, `gh pr create --label autofix` with a body saying `Closes #<n>`, that approval came from the issue's label and who placed it, and which verifications are workstation-only; `opsx-issue.sh phase <name> review`; never merge, never archive. Verify: the file names each step and `python3 .github/scripts/publication-guard.py` passes on it.
- [ ] 2.5 Create the label on the repository: `gh label create autoimplement --description "Approved for automatic implementation: a remote session proposes, implements and opens the pull request under autofix" --color <the autofix label's colour>`. Verify: `gh label list --json name -q '.[].name' | grep -x autoimplement`.

## 3. The loop fixes a red check (design D10)

- [ ] 3.1 Write `.github/scripts/failed-checks.py --pr <n> --sha <head> --out <file>`: through `gh api` lists the head's check runs, keeps conclusion `failure` among the checks `ci-green` depends on (read from `ci.yml`'s `needs:`, never hardcoded) excluding the review's own, fetches each run's `--log-failed` and keeps a tail bounded by `--tail-lines` (default 200), writes `[{kind: "check", job, run_url, tail}]`; an empty list when CI is green or has not reported, with `consulted` recorded as `sonar-issues.py` does. Verify: the suite in 5.3.
- [ ] 3.2 `review-dispatch.yml`: `workflow_run.workflows` gains `ci`, the `gate` admits a `ci` run with conclusion `failure` whose pull request carries the label (and still admits `claude-review` success as today); `collect` runs `failed-checks.py` under `actions: read` — that job only — and merges the items into `work-list.json` after the threads and the analysis issues, printing the three counts; the dispute path for a `check` item is one pull request comment naming the job, as for an analysis issue; the header comment gains the paragraph on why a red check is the loop's. Verify: the suite in 5.4.
- [ ] 3.3 The fixer's role file and `review-prompt.py`'s fix prompt: a `check` item is a finding whose text is the log tail; the fixer runs the job's own command from `ci.yml` in its checkout to reproduce, fixes, re-runs it before cutting the patch, or disputes naming what the log said and why the tree is not the cause (an outage, a rate limit, a flake). Verify: the role file names reproduce-first and the two outcomes; the prompt test in 5.4 asserts a `check` item's shape reaches the fixer.
- [ ] 3.4 `land-dispatch.py`: the round's summary counts checks fixed and disputed beside findings and analysis issues; a fixed check gets no reply (the next CI run is the verdict); rounds are counted from the same markers whichever event started them. Verify: `land-dispatch.test.sh` asserts the summary line with a check item present and absent.
- [ ] 3.5 `.github/routines/implement-issue.md` (from 2.4): the session opens the pull request and STOPS — it does not wait for CI or the review, and the file says the loop owns green from there; `.claude/rules/remote-session.md` (from 1.4) states the platform's own auto-fix stays OFF for this repository and why. Verify: `grep -c 'stops' .github/routines/implement-issue.md` ≥ 1 and `grep -ci 'auto-fix' .claude/rules/remote-session.md` ≥ 1.

## 4. The environment and the routine, outside the tree (design D2, D6, D8, D9, Migration)

- [ ] 4.1 Create the `agent-ops-operator` cloud environment at claude.ai/code: network Full; variables `SONAR_ORG=<org>` and `KUBEBUILDER_ASSETS=/home/user/.envtest/k8s/1.31.0-linux-amd64` (the path 1.1 prints); the Sonar token as an API credential for the SonarCloud API host; setup field exactly `export CLAUDE_CODE_REMOTE=1` newline `cd /home/user/agent-ops-operator && bash .github/scripts/cloud-bootstrap.sh`. Verify: a session started on the environment shows the verify hook's lines all `present` and `helm version --short` answers.
- [ ] 4.2 Create the routine (RemoteTrigger `create`, or the web form) on that environment and this repository, model per the project's review flags, saved prompt per D2 (three sentences pointing at `.github/routines/implement-issue.md`, naming the payload as a number, the issue as a request), `allowed_push_branches` `change/*` where accepted; add an API trigger and generate its token once. Verify: RemoteTrigger `get` shows the environment, the repository source and the prompt; `list_runs` is empty.
- [ ] 4.3 Store `ROUTINE_FIRE_URL` as a repository variable (`gh variable set`) and `ROUTINE_FIRE_TOKEN` as a repository secret (`gh secret set`). Verify: `gh variable list` shows the variable; `gh secret list` shows the secret; neither value appears anywhere in the tree (`git grep -c trig_` is 0).
- [ ] 4.4 Live proof, once: open a throwaway issue from the feature template describing a one-file change, place `autoimplement` as the owner, and watch: the workflow fires and comments the session link; the session promotes the issue, proposes, implements on `change/<n>-…`, opens the pull request with `Closes #<n>` and `autofix`, advances the phase; the review runs and the fixing loop answers it; then break one test on the branch by hand and watch the red `ci-green` start a round that fixes or disputes it. Then close the pull request, delete the branch, close the issue. Verify: the issue holds exactly one fire comment and the promotion comment; the pull request carried the label at creation (`gh pr view --json labels`); the gate refuses a label placed by a read-only test account or the check is recorded as not testable here.
- [ ] 4.5 Settle the open questions in the same session and record them: the Sonar MCP header behaviour (fix `.mcp.json` if the proxy needs the header absent), the image's Go version, whether the first push of a new branch is accepted from an interactive session — each one line in `gotchas.md` or `remote-session.md`. Verify: `git grep -n 'settled 2026-09' .claude/rules/` finds each.

## 5. Unit tests

- [ ] 5.1 New `.github/tests/cloud-bootstrap.test.sh`, wired into `run.sh`: marker unset → no installer called, exit 0; marker `0`/`false` → same; marker `1` with every binary stubbed present → no installer called, exit 0; marker `true` with helm absent and its installer stubbed to fail → the other sections still run, stderr names helm, exit 0; `--verify` prints one line per tool and calls nothing. Installers are stubbed through a `PATH` of fake binaries, no network. Verify: `.github/tests/run.sh` passes.
- [ ] 5.2 New `.github/tests/remote-implement.test.sh`, wired into `run.sh`, with a stubbed `gh` and a local fake fire endpoint (`python3 -m http.server` handler or a `--fire-url` pointing at a file-backed stub): the wrong label → exits 0, nothing called; a `read`/`triage` sender → label removed, one comment, no fire; a `write` sender → one POST carrying the number and the beta header, one comment with the returned URL under the marker; a 5xx from the endpoint → comment with the status, exit 1; a payload whose number is not numeric never reaches the fire. Verify: `run.sh` passes.
- [ ] 5.3 New `.github/tests/failed-checks.test.sh`, wired into `run.sh`, with a stubbed `gh`: a green head → empty list, consulted; one failed job among `ci-green`'s needs → one item with the job name, run url and a tail cut at the bound; a failed check outside the needs (the review's) → excluded; CI not yet reported → empty and not consulted; the needs are read from a fixture `ci.yml`. Verify: `run.sh` passes.
- [ ] 5.4 Extend `review-dispatch.test.sh`: the new workflow's assertions (top-level `permissions: {}`, the single job holds `issues: write` and nothing else, the trigger set is `issues.labeled` alone); and for `review-dispatch.yml`: `workflow_run.workflows` holds `ci` and `claude-review`, the gate admits `ci` only on `failure` and only labelled, `actions: read` appears on `collect` and on no other job, the work list merges three sources in order, and `pull_request_target` appears in no workflow. Verify: `run.sh` passes.
- [ ] 5.5 Run the whole suite and the two guards from the working copy: `.github/tests/run.sh`, `python3 .github/scripts/publication-guard.py`, `python3 .github/scripts/retired-vocabulary-guard.py`; and the CI `scripts` job's coverage step locally per `build-test.md`'s Python row, so the new scripts are measured. Verify: all exit 0 and `.github/coverage.xml` names the three new files.

## 6. E2E tests

- [ ] 6.1 Not applicable: nothing here is decided by a cluster — the change is a shell bootstrap, two workflows and the programs they run, an instruction file and rules, and every program's behaviour is pinned by section 5. The one live proof this change owes is not a cluster's but the platform's, and it is task 4.4: a labelled issue becomes a pull request under `autofix` through the real routine, the real environment and the real review, and a red check on it is fixed or disputed by the loop. Verify: this task claims nothing a cluster decides.

## 7. Documentation

### 7.1 Reference docs

- [ ] 7.1.1 `.claude/rules/worktree-delivery.md`: the consent table gains the `autoimplement` row (placed on an ISSUE by write access; starts a remote session; the pull request arrives under `autofix`) and its `autofix` row says every open finding, every open analysis issue AND every failed required check is fixed or disputed; "THE REVIEW FOUND SOMETHING" names a red `ci-green` as a round's start; the lifecycle section states that a remote session's clone is the working copy and points at `remote-session.md`; the archive rule is unchanged and says so. Verify: the row is present and `grep -c 'remote-session.md' .claude/rules/worktree-delivery.md` ≥ 1.
- [ ] 7.1.2 `.claude/rules/build-test.md` and `gotchas.md` carry the corrections from 1.5, re-read after implementation against what the first remote session actually found (the Go version, the push behaviour), so the rule states the measured fact and not the plan. Verify: each measured item from 4.5 is present in one of the two files.
- [ ] 7.1.3 `CONTRIBUTING.md`: "Start with an issue or a discussion" gains one paragraph on the `autoimplement` label — who may place it, what happens, that the pull request is still reviewed and merged by a person; "One change, one worktree, one branch, one pull request" gains the remote session as the other shape of the working copy; "Pull requests" says the labelled loop fixes failed checks beside findings and analysis issues. Verify: `grep -c autoimplement CONTRIBUTING.md` ≥ 2 and `grep -c 'failed check' CONTRIBUTING.md` ≥ 1.
- [ ] 7.1.4 The four delta specs are archived into `openspec/specs/` by `/opsx:archive` on the branch; `openspec validate --all` passes before that. Verify: the command exits 0.

### 7.2 Adopter site

- [ ] 7.2.1 None of the landing page, introduction, getting started, installation, the integration pages or the guides describes how this project is developed, and no shipped behaviour changed — so no site page is touched, stated here as a claim a reviewer can dispute rather than an omission. Verify: `git diff --stat master -- docs/` is empty for this change.
