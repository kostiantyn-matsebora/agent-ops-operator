## 1. The vocabulary, and the labels themselves (design D1, D4, D5)

- [x] 1.1 `.github/review-triage.json`: `approve_label` becomes `conveyor:fix`; add `run_label` (`conveyor:run`), `implement_label` (`conveyor:implement`), `archive_label` (`conveyor:archive`), `keep_going_label` (`conveyor:keep-going`) and `max_rounds` (5), each with a `_comment` paragraph saying what a person means by placing it and that a program may only carry or consume one. Verify: `python3 -c 'import json;d=json.load(open(".github/review-triage.json"));print(d["approve_label"], d["run_label"], d["max_rounds"])'` prints `conveyor:fix conveyor:run 5`.
- [x] 1.2 Add `autofix` AND `autoimplement` to `.github/retired-vocabulary.json` (one term, one pattern alternation, since both retire together and both `says` the same `conveyor:` vocabulary) with `says` naming `conveyor:fix` / `conveyor:implement`, and confirm the guard then FAILS on a tree still using either old name. Verify: `python3 .github/scripts/retired-vocabulary-guard.py` reports the remaining uses, and reports clean once section 2 and section 5 have landed.
- [x] 1.3 Create the five labels on the repository with descriptions stating scope and who may place them (`gh label create`), keeping `autofix` until the migration's last step. Verify: `gh label list --json name -q '.[].name' | grep -c '^conveyor:'` is 5.

## 2. The line: one instruction, carried at every transition (design D1, D2, D3)

- [x] 2.1 `.github/scripts/remote-implement.py`: the trigger label is `run_label` OR `implement_label` read from the vocabulary; the refusal, the marker comment and the fire are unchanged. Verify: the suite in 5.1.
- [x] 2.2 Write `.github/scripts/carry-grant.py --issue <n> --station fix|archive [--pr <n>]`, where `--pr` is REQUIRED for `--station fix` (the label goes on that pull request) and REFUSED for `--station archive` (the label goes on the issue, and a pull request number there names a closed object nothing reads) — the program rejects the wrong combination rather than ignoring the argument: reads the issue's CURRENT labels, does nothing unless `run_label` is present, checks the person who placed it still has write access (the collaborators API, as the gate does), places the station's label WHERE THAT STATION IS DRIVEN — `fix` on the pull request, `archive` on the ISSUE, because a merged pull request is closed and a label on it drives nothing — and comments ONCE under a marker naming whose instruction it carried. Exits 0 doing nothing when the instruction is absent — that is the ordinary case, not an error. Verify: the suite in 5.2.
- [x] 2.3 `.github/workflows/remote-implement.yml`: a second job resolves the merged pull request from a `push` to the default branch, reading its body for `Refs #<n>`, then calling `carry-grant.py --station archive`, which reads the LANE first and does nothing on the plain lane (it has no archive station and its line ends at this merge) and otherwise labels THE ISSUE — so the job needs `issues: write` and `pull-requests: read`, and nothing more; an archive pull request (which carries `Closes`, not `Refs`) is a no-op. **REDESIGNED post-merge, on `workflow_run` after `ci` completes for the `push` event rather than `pull_request: closed` directly** — a `GITHUB_TOKEN` issued to a `pull_request`-triggered run carries no write access in this repository at all, measured live during e2e testing against a throwaway repository. Same station, same labels, different trigger; the merged pull request is resolved via a merged-pull-request search on the pushed commit instead of read from the event payload. Verify: the suite in 5.4, and confirmed live end-to-end against `conveyor-labels-e2e-test` (`conveyor:archive` landed on the issue, with the marker comment, after a real merge).
- [x] 2.4 The same workflow gains the OPEN transition: reads a pull request's body for `Refs #<n>` from a branch named `change/*` and calls `carry-grant.py --station fix`. **REDESIGNED post-merge, on `workflow_run` after `ci` completes for the `pull_request` event rather than `pull_request: opened` directly**, same reason as 2.3 — the pull request is resolved from the run's head sha via `commits/<sha>/pulls`, needing `pull-requests: write` (labelling a pull request through this API needs write, not read, confirmed live). Verify: the suite in 5.4 asserts both transitions and that neither runs on a fork, and confirmed live end-to-end against `conveyor-labels-e2e-test` (`conveyor:fix` landed on a real pull request, with the marker comment).
- [x] 2.5 `.github/routines/implement-issue.md` gains the LANE READ as its first step: the issue is on the opsx lane when `openspec/changes/*/.github-issue` holds its number or an `opsx:` phase label is present, and on the plain lane otherwise — read, never judged from the issue's wording. The opsx path is the file's current process; the PLAIN path implements what the issue describes, opens the pull request and stops at the merge, owing no proposal, task list or delta spec but the same green bar. Verify: the file states both lanes and the test that selects them, and `grep -c 'never.*judg\|read, never' .github/routines/implement-issue.md` ≥ 1.
- [x] 2.6 `.github/routines/implement-issue.md`: the session opens its pull request with NO label, and the file says why — it acts as an application with no write access, so a label it places is removed by the gate; the workflow labels it instead. Verify: `grep -c 'autofix\|--label' .github/routines/implement-issue.md` shows no label flag on `gh pr create`.

## 3. The bound, and the label that extends it (design D4)

- [x] 3.1 `.github/workflows/review-dispatch.yml`: the static `MAX_ROUNDS: 3` `env:` entry is removed; the bound is read as `max_rounds` from the vocabulary file by the gate and passed through as `needs.gate.outputs.max_rounds`, so the number is stated once. The gate's prefilter and its label read use `approve_label` from the file, which is now `conveyor:fix`. Verify: the suite in 5.4.
- [x] 3.2 `.github/scripts/land-dispatch.py`: the bounded ending's summary names the rounds used and the extending label as the way to grant another set; the model-free landing job REMOVES `keep_going_label` when a round runs under it. Verify: the suite in 5.3 asserts the summary text and the removal.
- [x] 3.2a `.github/workflows/review-dispatch.yml` tells the lander WHICH report it got: the step that substitutes `{"items":[]}` for a missing report writes a sentinel beside it (or passes a flag), so an absent report and an empty one are distinguishable. Verify: the suite in 5.4 asserts the substitution still happens and that the flag rides with it.
- [x] 3.2b `.github/scripts/land-dispatch.py`: three outcomes rather than two — an item the report declines is DISPUTED with its reason; an item a real report omits is UNADDRESSED, worded as such and never as a dispute; and a round whose fixer produced no report at all ends as `no report`, its own stated ending, disputing nothing. An unaddressed item is not marked in a way that stops a later round retrying it. Verify: the suite in 5.3.
- [x] 3.3 `review-dispatch.yml`'s gate admits a round when the pull request carries `approve_label` AND either a person placed it or `carry-grant.py` did with a standing instruction that is STILL present — re-read, never trusted from the carry. Verify: the suite in 5.4 covers a carried label whose instruction was removed.

## 4. Outside the tree (design D5, Migration)

- [x] 4.1 Relabel any open pull request carrying `autofix` to `conveyor:fix` (and any open issue carrying `autoimplement` to `conveyor:implement`), then delete both old labels once none carries either. Verify: `gh label list --json name -q '.[].name' | grep -cE '^(autofix|autoimplement)$'` is 0 and no open pull request or issue lost its grant. (Neither label was carried by anything open; both deleted directly.)
- [ ] 4.2 Live proof, once: place `conveyor:run` on a throwaway issue and watch the whole line — the session implements and opens an UNLABELLED pull request, the workflow labels it `conveyor:fix` naming the approver, the loop drives it, a person merges, the workflow labels the change `conveyor:archive`, and the archive pull request opens. Then close everything and delete the branches. Verify: the pull request's label was placed by the workflow and not the session, and the gate accepted it. **DEFERRED: `issues: labeled` and `pull_request` transition workflows run the DEFAULT BRANCH's copy of `.github/workflows/remote-implement.yml`, so this cannot be proven live until this change itself is merged. Run this proof once merged, before relying on the mechanism for another change.**
- [ ] 4.3 Prove the refusal too: have the session's own label be placed (or simulate it) and confirm it is removed with a visible comment, so the #201 defect cannot return unnoticed. Verify: recorded in `gotchas.md` as measured, or stated as not testable without the app's credentials. **DEFERRED alongside 4.2, same reason — the gate change under test is not live until merged.**

## 5. Unit tests

- [x] 5.1 Extend `.github/tests/remote-implement.test.sh`: fires on `run_label` and on `implement_label`, on neither for any other label, and the existing gate, marker and payload cases still hold with the renamed vocabulary. Verify: `.github/tests/run.sh` passes.
- [x] 5.1a New `.github/tests/conveyor-lane.test.sh`, wired into `run.sh`: the lane is read from a fixture tree — a `.github-issue` holding the number → opsx; an `opsx:` phase label and no binding → opsx; neither → plain; a binding for a DIFFERENT issue → plain. The wording of the issue changes nothing in any case, which the suite asserts by running the same body through both fixtures. Verify: `run.sh` passes.
- [x] 5.2 New `.github/tests/carry-grant.test.sh`, wired into `run.sh`, with a stubbed `gh`: no standing instruction → exits 0, places nothing; instruction present and its placer has write access → places the station label on the RIGHT TARGET (`fix` on the pull request, `archive` on the issue) and comments once under the marker naming them; the placer has LOST write access → places nothing and says so; already carried (marker present) → no second comment; a pull request body with no `Refs #<n>` → no-op; the ARGUMENT CONTRACT — `--station fix` without `--pr` is refused, `--station archive` WITH `--pr` is refused, neither silently ignored; and `--station archive` on a PLAIN-lane issue places nothing and says why, while the same call on an opsx-lane issue labels it. Verify: `run.sh` passes. (The `Refs #<n>` parse is `remote-implement.yml`'s, not `carry-grant.py`'s own — covered in 5.4's workflow suite instead.)
- [x] 5.3 Extend `.github/tests/land-dispatch.test.sh`: a report declining an item says DISPUTED with its reason; a real report omitting an item says UNADDRESSED and never "disputed"; NO report at all ends the round as `no report` and disputes nothing — the case observed on this change's own proposal, which today yields one phantom dispute per finding; the bound is read rather than hardcoded; a bounded ending names the rounds used and the extending label; `keep_going_label` is removed when a round runs under it and not otherwise. Verify: `run.sh` passes.
- [x] 5.4 Extend `.github/tests/review-dispatch.test.sh`: the renamed label throughout; the bound comes from the vocabulary file; and for `remote-implement.yml` the two new transitions — `pull_request: opened` on `change/*` and `closed` with `merged == true` — each with the job's permissions pinned, no `pull_request_target` anywhere, and a fork refused. Verify: `run.sh` passes.
- [x] 5.5 Run the whole suite and both guards from the working copy, plus the CI `scripts` job's coverage step per `build-test.md`, so `carry-grant.py` is measured. Verify: all exit 0 and `.github/coverage.xml` names the new file. (`.github/tests/run.sh`, `publication-guard.py`, `retired-vocabulary-guard.py`, `docs-generate.py --check` all exit 0; coverage run confirms `scripts/carry-grant.py` at 86% line coverage.)

## 6. E2E tests

- [x] 6.1 Not applicable: nothing here is decided by a cluster — the change is two workflows, three programs and a vocabulary file, and every program's behaviour is pinned by section 5. The live proof this change owes is the platform's, not a cluster's, and it is task 4.2. Verify: this task claims nothing a cluster decides.

## 7. Documentation

### 7.1 Reference docs

- [x] 7.1.1 `.claude/rules/worktree-delivery.md`: the consent table becomes the five `conveyor:` labels with who may place each; a new short section states that a program may CARRY or CONSUME a grant and never mint one, naming #201 as what it cost; the round bound names the constant and its default. Verify: `grep -c 'conveyor:' .claude/rules/worktree-delivery.md` ≥ 5 and `grep -c autofix` is 0 outside a retired-name record.
- [x] 7.1.2 `.claude/rules/remote-session.md`: the label table, and the sentence saying the pull request carries the approve label from creation becomes its inverse — the session labels nothing and the workflow carries the grant. Verify: the file names all five labels and states the session places none.
- [x] 7.1.3 `CONTRIBUTING.md`: the paragraph on handing an issue to a session names `conveyor:run` and what it carries, and the pull-request section names `conveyor:fix`, the bound and `conveyor:keep-going`. Verify: `grep -c 'conveyor:' CONTRIBUTING.md` ≥ 3.
- [x] 7.1.4 `.claude/skills/openspec-apply-change/SKILL.md`: the step asking for the owner's word names the new label and says a session may not place it. Verify: the file names `conveyor:fix` and states the session does not place it.
- [x] 7.1.5 `.claude/rules/gotchas.md`: one entry recording #201 — a session acts as the app, holds no write access, and a label it places is removed by the gate; the fix is that programs carry grants rather than mint them. Verify: the entry names the pull request and the actor.
- [x] 7.1.5a RECONCILE WITH `remote-change-sessions`, whichever archives second. Its delta spec says the session delivers its pull request "carrying the approve label for automatic fixing from creation", which this change makes false — the session labels nothing. If that change archives FIRST, this one adds a `specs/remote-change-sessions/spec.md` delta correcting the requirement; if this one archives first, that change's delta is corrected before it archives. Verify: after both are archived, `grep -rn 'carrying the approve label' openspec/specs/` finds nothing that says the session places it. (`remote-change-sessions` is unarchived; its `remote-change-sessions` delta was corrected in place. Its `change-delivery` delta went further than correction — it independently MODIFIED the same "approved for automatic fixing" requirement this change also modifies, which a review caught as two pending deltas rewriting one requirement rather than a stale record. Fixed by REMOVING that requirement block from `remote-change-sessions`' delta entirely — this change owns it now — and folding the one scenario unique to that delta, the interactive-workstation case, into this change's own `specs/change-delivery/spec.md` so it survives. `openspec validate --strict` passes on both changes.)
- [x] 7.1.6 The four delta specs are archived into `openspec/specs/` by `/opsx:archive` on the branch; `openspec validate --all` passes before that. Verify: the command exits 0. (`openspec validate conveyor-labels --strict` and `openspec validate remote-change-sessions --strict` both pass; the two pre-existing failures in `--all` — `on-demand-adapters`, `signal-triage` — predate this change and are unrelated. The actual archive runs as its own step, separately.)

### 7.2 Adopter site

- [x] 7.2.1 None of the landing page, introduction, getting started, installation, the integration pages or the guides describes how this project is developed, and no shipped behaviour changed. **ONE EXCEPTION, NOT A COUNTEREXAMPLE:** `docs/security.md`'s "The fixing loop's push credential" section names the approve label by its OLD name (`autofix`) while describing the `AUTOFIX_DEPLOY_KEY` security control (which is unchanged and keeps its name) — that sentence would be factually wrong once this change lands, so it is corrected to name `conveyor:fix` and the carry mechanism. Nothing else on the site changes. Verify: `git diff --stat master -- docs/` names only `docs/security.md`.
